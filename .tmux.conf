unbind C-b
set -g prefix C-o
bind C-o send-prefix

set -s escape-time 1

set -g base-index 1
setw -g pane-base-index 1

set -g default-terminal "screen-256color"

bind r source-file ~/.tmux.conf \; display "Reloaded!"

bind | split-window -h
bind - split-window -v

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

setw -g monitor-activity on
set -g visual-activity on

if-shell "command -v powerline-daemon &>/dev/null" 'run-shell "powerline-daemon -q"'
if-shell "[[ -r $(python -m site --user-site)/powerline/bindings/tmux/powerline.conf ]]" 'source "$(python -m site --user-site)/powerline/bindings/tmux/powerline.conf"'

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'

run '~/.tmux/plugins/tpm/tpm'
